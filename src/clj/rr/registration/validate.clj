(ns rr.registration.validate)

;; verify that the registration params work correctly.


;; For docker - pull image, then start container
;; For lambda - just execute functions

;; Should be able to use rr.runner to test
;; Will need to refactor game-viewer to pull out bot creation logic to bots.cljc

